const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const el={addEventListener(){}};const c=vm.createContext({document:{querySelector:()=>el,addEventListener(){}},window:{addEventListener(){}},indexedDB:{open(){return {}}},structuredClone,console,Map,Set,Promise});vm.runInContext(fs.readFileSync('outputs/storyforge/tail-qte.js','utf8'),c);vm.runInContext(fs.readFileSync('outputs/storyforge/app.js','utf8').replace('render()}init();','render()}'),c);vm.runInContext(`const p=structuredClone(initial);configureChapterOne(p);installNineVideos(p);applyQteTimingDefaults(p);installDeathNode(p);const death=p.nodes.find(n=>n.deathScreen);globalThis.result=valid(p)&&p.nodes.filter(n=>n.type==='qte').every(n=>n.failure===death.id&&n.qteFailure==='branch'&&n.limit===4)&&death.options[0].target===p.nodes[0].id&&!installDeathNode(p);`,c);assert(c.result);console.log('PASS all QTE fail to death, restart destination, 4s preserved, valid configuration, migration runs once');
