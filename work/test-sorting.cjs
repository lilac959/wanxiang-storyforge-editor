const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const c=vm.createContext({document:{addEventListener(){}}});vm.runInContext(fs.readFileSync('outputs/storyforge/sorting.js','utf8'),c);const list=['a','b','c','d'].map(id=>({id,duration:3}));assert(c.moveSortedItem(list,'a','d',true));assert.equal(list.map(x=>x.id).join(''),'bcda');assert(c.moveSortedItem(list,'a','b',false));assert.equal(list.map(x=>x.id).join(''),'abcd');assert(!c.moveSortedItem(list,'b','c',false));assert(!c.moveSortedItem(list,'z','c',true));assert(!c.moveSortedItem(list,'a','a',true));assert(list.every(x=>x.duration===3));console.log('PASS reorder forward/backward, insertion edges, no-op, missing IDs, duration preservation');
