const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const el={addEventListener(){}};const c=vm.createContext({document:{querySelector:()=>el,addEventListener(){}},window:{addEventListener(){}},indexedDB:{open(){return {}}},structuredClone,console,Map,Set,Promise});vm.runInContext(fs.readFileSync('outputs/storyforge/tail-qte.js','utf8'),c);vm.runInContext(fs.readFileSync('outputs/storyforge/app.js','utf8').replace('render()}init();','render()}'),c);vm.runInContext(`const p=structuredClone(initial);configureChapterOne(p);installNineVideos(p);applyQteTimingDefaults(p);repairTiming(p);globalThis.ok=valid(p)&&p.nodes.filter(n=>n.type==='qte').every(n=>n.trigger===2&&n.limit===4);p.nodes.find(n=>n.type==='qte').limit=5;applyQteTimingDefaults(p);globalThis.kept=p.nodes.find(n=>n.type==='qte').limit===5`,c);assert(c.ok&&c.kept);console.log('PASS all QTE 2s/4s, short videos remain saveable, later edits preserved');
