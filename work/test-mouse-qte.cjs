const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const ctx=vm.createContext({window:{addEventListener(){},removeEventListener(){}},performance,clearInterval,setInterval});vm.runInContext(fs.readFileSync('outputs/storyforge/mouse-qte.js','utf8'),ctx);function run(mode,moves,expected){const handlers={};let wins=0;const root={addEventListener(k,v){handlers[k]=v},removeEventListener(){},setPointerCapture(){}};const clean=ctx.bindMouseQte({qteGesture:mode,qteClicks:3,qteDistance:80},root,()=>true,()=>wins++,()=>{});for(const [x,y] of moves){const e={button:0,pointerId:1,clientX:0,clientY:0,target:{closest:()=>null},preventDefault(){}};handlers.pointerdown(e);handlers.pointerup({...e,clientX:x,clientY:y})}assert.equal(wins,expected);clean()}run('click',[[0,0]],1);run('multi',[[0,0],[0,0],[0,0]],1);run('right',[[-100,0],[100,0]],1);run('up',[[0,100],[0,-100]],1);run('left',[[100,0],[-100,0]],1);run('down',[[0,-100],[0,100]],1);console.log('PASS click, click count, all directions and wrong-direction rejection');
