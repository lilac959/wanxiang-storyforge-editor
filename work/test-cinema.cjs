const {chromium}=require('C:/Users/Admin/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4173/');await p.waitForSelector('.nodeitem');const result=await p.evaluate(()=>{const n=project.nodes.find(n=>n.video==='asset-tail-video-7');selected=n.id;page='story';render();const stage=document.querySelector('#editorArea .stage');const heights=[0,.4,1,20,21,23.635].map(t=>{syncCinema(stage,n,t,25.635);return parseFloat(stage.querySelector('.cinema-bars').style.getPropertyValue('--cinema-height'))});return {enabled:n.cinemaMode,heights}});assert(result.enabled);assert.equal(result.heights[0],0);assert(result.heights[1]>0&&result.heights[1]<12);assert.equal(result.heights[2],12);assert.equal(result.heights[4],0);assert.equal(result.heights[5],0);await p.locator('[data-key="cinemaMode"]').selectOption('off');assert.equal(await p.locator('.cinema-bars').count(),0);await p.locator('[data-key="cinemaMode"]').selectOption('on');assert(await p.evaluate(()=>current().cinemaMode===true));console.log('PASS clip7 defaults, animated entry, pre-QTE full frame, editor toggle');await b.close()})().catch(e=>{console.error(e);process.exit(1)});
